<?php
	// Start with a function that takes one argument. As you continue through the problem, your function will take more arguments eg:
	// function dateCalculator($year, $month) {};
	// function dateCalculator($year, $month, $day) {};
	function dateCalculator($year) {
		// Your function will be written here.
	}

	// Call the function, passing in a year to start. As you continue through the problem, you will need to pass more arguments to the function.
	echo dateCalculator(1999);
?>